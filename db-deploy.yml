apiVersion: apps/v1
kind: Deployment
metadata:
  name: mysql
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mysql
  template:
    metadata:
      labels:
        app: mysql
    spec:
      containers:
        - name: mysql
          image: roylatin/dockerdb
          env:
            - name: MYSQL_ROOT_PASSWORD
              value: password  # Replace with your desired MySQL root password
            - name: MYSQL_DATABASE  # Corrected line with a space after the dash
              value: crypto
          ports:
            - containerPort: 3306
          volumeMounts:
            - name: mysql-persistent-storage
              mountPath: /var/lib/mysql
      volumes:
        - name: mysql-persistent-storage
          emptyDir: {}
